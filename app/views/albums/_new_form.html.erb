<%= form_for(@album) do |f|%>
    <%= f.label :albumname %>
    <%= f.text_field :album_name %>

    <%= f.hidden_field(:main, value: false) %>
    <%= f.hidden_field(:user_id, value: current_user.id) %>
    
    <%= f.submit "Save Album" %>
<% end %>